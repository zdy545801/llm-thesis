# 50 页扩展执行包（从当前进度直接推进）

你现在的基础已经足够写出完整论文。接下来按下面四个阶段推进，可以把“实验 + 分析 + 应用化”扩展到 50 页且逻辑完整。

## 阶段 A：补齐实验维度（2-4 天）

### A1. 方法维度补齐（MEND）
- 目标：形成 `FT / MEMIT / ROME / MEND` 四方法同口径对比。
- 最小可行版本：
  1. `MEND-R1`：10 轮（同 FT 对比参数）
  2. `MEND-R2`：重复一遍（验证稳定性）
- 结果目录建议：
  - `results/MEND/MEND_gpt2_ft_compare_10rounds_YYYYMMDD`
  - `results/MEND/MEND_gpt2_ft_compare_10rounds_YYYYMMDD_r2`

### A2. 数据维度扩展（sample -> full）
- 目标：验证结论在更大样本下是否仍成立。
- 建议先做 1 个方法（FT）+ 1 个方法（MEMIT）：
  - `crows_pairs_full.jsonl`
  - `bbq_full.jsonl`

### A3. 分层分析（类别）
- 目标：提高“分析深度”而非只报总分。
- BBQ 可按 `data/bbq_full_categories/*.jsonl` 分类别评测：
  - Race、Gender、Religion、Age、SES 等。

---

## 阶段 B：补齐统计与稳健性（1-2 天）

### B1. 方法级自动汇总
- 已新增脚本：`scripts/summarize_method_runs.py`
- 运行：

```powershell
python scripts/summarize_method_runs.py
```

- 产出：
  - `results/method_run_stats.csv`
  - `results/method_run_stats.md`

### B2. 编辑成功率 + 漂移联动
- 你已有：
  - `results/edit_success_summary.csv`
  - `results/edit_success_summary.md`
- 建议在结果章节新增一个小节：
  - `ESR` 与 `delta_last` / `fdr_abs_mean` 关系讨论（有效性-公平性权衡）。

---

## 阶段 C：应用化落地（1 天）

把论文创新点从“仅实验”提升到“可落地流程”：

### C1. 公平性体检流程（你可直接写成系统原型）
- 输入：模型 + 编辑请求集 + 公平性测评集
- 过程：轮次编辑 -> 每轮评测 -> 漂移计算 -> 报告生成
- 输出：
  - `drift_metrics.json`
  - `drift_curve.png`
  - `edit_success_summary.csv`
  - 方法级汇总表（`method_run_stats.csv`）

### C2. 实际价值陈述（写在摘要/结论）
- 可用于开源模型发布前的风险体检
- 可用于模型更新后的回归检查
- 可用于对比不同编辑方法的风险画像

---

## 阶段 D：写作页数分配（目标 50 页）

建议结构（可直接照搬）：

1. 绪论（6-8 页）
2. 相关工作（8-10 页）
3. 问题定义与指标（6-8 页）
4. 实验设置（8-10 页）
5. 结果与分析（12-14 页）
6. 应用化流程与案例（4-6 页）
7. 结论与展望（2-3 页）

---

## 本周执行清单（直接打勾）

- [ ] 跑 `MEND-R1`（10 轮）
- [ ] 跑 `MEND-R2`（10 轮）
- [ ] 跑 1 组 full 数据实验（优先 FT）
- [ ] 运行 `python scripts/summarize_method_runs.py`
- [ ] 更新 `实验结果总表.md`（加入 MEND + full）
- [ ] 更新 `paper_final/chapter_results_analysis_draft.md`（加“机制解释 + 应用化”）

---

## 你现在最优先的顺序（今天）

1. 先跑 `MEND-R1`（拿到第四方法）
2. 跑 `python scripts/summarize_method_runs.py`（拿到统计表）
3. 把新增结果写进 `实验结果总表.md`

只要完成这三步，你的论文完整度会明显上一个台阶。

