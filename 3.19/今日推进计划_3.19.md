# 今日推进计划（3.19）

## 一、当前状态（已确认）
1. 已有基线与轮次结果：`baseline_gpt2.json`、`round_0~2.json`、`drift_metrics.json`、`drift_curve.png`。
2. 已有方法/数据/流程/复现日志文稿，可直接扩展为完整实验章。
3. 当前主要瓶颈仍是 GPU 资源不稳定，因此今天优先做“无GPU高产出任务”。

## 二、今天目标（可交付）
1. 完成“结果与分析”章节初稿（含指标解释、趋势解读、威胁与局限）。
2. 生成一版可直接汇报的“阶段性结论 + 下一步实验计划”。
3. 准备 GPU 恢复后的一键执行清单，避免到时临场排错。

## 三、今天执行清单（按顺序）
1. 把现有三轮结果写成表格（Round、CrowS、BBQ、Risk、FDR）。
2. 完成结果解读：
   - 公平风险是否单调上升；
   - 漂移速度是否加快；
   - 累积漂移（CDA）是否显著。
3. 写“现实意义”段落：把研究价值映射到上线模型更新质检流程。
4. 写“局限性”段落：样本量小、单模型、单方法、CPU近似评估。
5. 写“GPU恢复后48小时计划”：补跑FT多轮 + 增加一个对照方法（ROME/MEMIT二选一）。

## 四、可直接汇报给老师的话术
在无GPU条件下，我已完成可复现的公平性漂移评估闭环：包括基线评估、多轮结果记录、漂移指标计算（FDR/CDA）和趋势可视化。现有结果显示公平风险随编辑轮次上升，验证了“连续知识编辑可能引发公平性恶化”的研究假设。下一步在GPU恢复后补充更大样本与对照方法实验，用于形成完整结果章与消融分析。

## 五、GPU恢复后48小时落地清单
1. 跑 FT 连续编辑到至少 5 轮，输出 `results/rounds/round_*.json`。
2. 重新计算 `drift_metrics.json` 并更新 `drift_curve.png`。
3. 增加 1 个对照方法（优先 ROME，若依赖冲突则换 MEMIT）。
4. 每组最少重复 2 次（不同 seed 或编辑顺序）并报告均值。
5. 回填论文结果表和“方法比较”小节。
