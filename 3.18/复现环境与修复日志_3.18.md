# 复现环境与修复日志（2026-03-18）

## 一、今日完成
1. 在 CPU 环境完成 baseline 公平性评估。  
2. 生成结果文件：`results/baseline_gpt2.json`。  
3. 梳理 Colab 依赖冲突与稳定化策略。  
4. 产出论文可用文本与表格骨架。

## 二、关键结果（CPU）
- 模型：GPT-2  
- 设备：CPU  
- CrowS(sample)：`prefer_stereo_rate = 0.6`（valid=5）  
- BBQ(sample)：`accuracy_proxy = 0.2`（valid=5）

## 三、遇到的问题
1. Colab GPU配额限制，无法连接 T4。  
2. EasyEdit 默认导入链过重，触发多余模块兼容问题。  
3. `transformers` / `sentence-transformers` / `huggingface_hub` 存在版本耦合冲突。

## 四、修复策略
1. 固定关键版本（主线使用 `transformers==4.40.2`）。  
2. Notebook 增加“启动即检查清单”（版本、补丁、文件哈希）。  
3. 自动搜索 Drive 项目路径，避免中文路径乱码。  
4. 主脚本重写为稳定版，避免多轮字符串补丁造成缩进污染。

## 五、下一步（GPU恢复后）
1. 运行 FT 连续编辑多轮（建议 3 轮起）。  
2. 计算 `drift_metrics.json` 并绘制漂移曲线。  
3. 增加 ROME/MEMIT 对照组，补全方法比较章节。
